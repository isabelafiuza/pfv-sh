% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.r
\name{run_plants}
\alias{run_plants}
\title{Despacha Processamento de Usinas (Sequencial ou Paralelo)}
\usage{
run_plants(v_usinas, fn, extra_args, parallel, metrics, lg)
}
\arguments{
\item{v_usinas}{character vector de IDs de usinas a processar}

\item{fn}{funcao worker a chamar para cada usina; assinatura
\code{fn(iu, ...)} onde \code{...} sao os campos de \code{extra_args}}

\item{extra_args}{lista nomeada de argumentos adicionais a passar para \code{fn}}

\item{parallel}{logical, se \code{TRUE} usa \code{future.apply::future_lapply}}

\item{metrics}{objeto de metricas criado por \code{\link[=create_metrics]{create_metrics()}}}

\item{lg}{objeto logger}
}
\value{
lista com os resultados por usina (na ordem de \code{v_usinas});
erros sao encapsulados como objetos \code{plant_error}
}
\description{
Itera sobre \code{v_usinas} chamando \code{fn} via \code{do.call}, com isolamento de erro
por usina via \code{plant_error}. No modo sequencial, o tempo por usina e medido
individualmente. No modo paralelo, o tempo total do lote e distribuido
igualmente.
}
